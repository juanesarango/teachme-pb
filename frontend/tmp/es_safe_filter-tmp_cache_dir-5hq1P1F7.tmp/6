import Ember from 'ember';

export default Ember.ObjectController.extend({
	isEditable: true,
	isAboutEditable: true,
	isFeeEditable: true,
  actions: {
    toogleEditable: function(property) {
         this.toggleProperty(property);
      }
    }
});
